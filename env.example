# OpenAI API Key
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model (optional)
# Default: gpt-3.5-turbo (widely available, fast, cost-effective)
# Options: gpt-3.5-turbo, gpt-4, gpt-4-turbo, gpt-4o, gpt-4o-mini
# If you have access to GPT-4, you can use: gpt-4, gpt-4-turbo, or gpt-4o
OPENAI_MODEL=gpt-3.5-turbo

# Database URL
# PostgreSQL connection string (e.g., from Neon, Vercel Postgres, etc.)
DATABASE_URL=your_database_url_here

# NextAuth Configuration
# For production, generate a secure random string
# You can use: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# Admin Registration Token
# Secret token for admin registration link
# Use the same as SEED_SECRET or generate a new one
# Format: /register-admin?token=YOUR_TOKEN_HERE
ADMIN_REGISTER_TOKEN=your_admin_register_token_here
